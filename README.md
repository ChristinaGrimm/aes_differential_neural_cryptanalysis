## Requirements

- Python 3
- NumPy
- TensorFlow

## Training

Run:

python deep_net_aes.py

Edit `SELECTED` to select the byte column and edit `group_sizes`
to select the number of ciphertext pairs per input instance.
